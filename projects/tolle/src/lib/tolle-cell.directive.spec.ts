import { TolleCellDirective } from './tolle-cell.directive';

describe('TolleCellDirective', () => {
  it('should create an instance', () => {
    const directive = new TolleCellDirective();
    expect(directive).toBeTruthy();
  });
});
